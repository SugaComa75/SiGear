After verification ID is stored with an encrypted file (recomend back is kept on a usb device like a yubikey)
using the contol app a copy can be stored with in the vault allowing avatar creation, policy rules, and other setting to be linked and transferred to the mobile app
user must connect with control app atvleast one per (week, month) to verify ID , failure todo so will cause the mobile app to lock the ID preventing access set in the user policy. 
