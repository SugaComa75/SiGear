Contracts: canonical schemas and API definitions

Purpose: Store shared OpenAPI specs, schema definitions, consent and audit record shapes used by all components. Implementations must import these contracts rather than redefining models.
