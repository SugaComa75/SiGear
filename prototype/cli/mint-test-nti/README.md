Mint-test-NTI CLI preserved in prototype for demo purposes.

Use the included mock server in `prototype/cli/mint-mock-server` or the original script to run a demo.

Example:

  node cli.js --tier low
